# site-a11y — 2026-08-30

Lane for q-auto-8cd1, q-auto-4f45, q-auto-acf7, q-auto-e7d0 on automancer.uk.
Clone: `/opt/automancer/clones/site-a11y-20260830`. Shared box; one browser session.

Session name: `site-a11y-20260830`. At most one browser. Do not `close --all`.

## Constraints

- Public website. Surgical fixes only. No redesign, rebrand, palette, type, or logo change.
- Do not deploy. End at a pushed branch with green gates.
- Do not invent a terms date. Derive from `git log -1 --format=%cs` on the terms page.
- Do not touch `/opt/automancer/ops`.
- Do not loosen a page-weight budget or an axe rule.
- Check each of the four against the live site before changing anything.

## Plan

1. Measure live site at 390×844 for tap targets, axe, terms date, footer logo `loading`.
2. Skip any item already fixed.
3. Apply surgical source fixes.
4. Re-measure on a local preview (visitor-browser, not source).
5. Run existing gates in ONE `campaign heavy` invocation.
6. Commit on `agent/q-auto-site-a11y` and push. Verify remote ref.

## Session log

- 21:59 UTC — notes file created (replaced earlier contrast-lane notes from q-auto-4145 that occupied this path).
- Live site measured in session `site-a11y-20260830` at CSS viewport 390×844 (`window.innerWidth/innerHeight`). Page-load positive control: title `AI & automation for UK small businesses | Automancer`, h1 `Your admin, automated into oblivion.`, body contains `Company No. 17060907`, `document.readyState === complete`.
- Chrome baseline before open: 14 processes matching `$1 ~ /chrome/`.

## Live-site premises (before any source change)

Tool: agent-browser 0.34.0, axe-core 4.12.1 (vendored in agent-browser `a11y`).

### q-auto-8cd1 — tap targets — CONFIRMED

`getBoundingClientRect` at 390×844 on https://automancer.uk/ and /contact/:

| target | page | box (CSS px) |
| --- | --- | --- |
| Privacy | every (footer) | 48.38 × 18.42 |
| Terms | every (footer) | 34.56 × 18.42 |
| llms.txt | every (footer) | 55.28 × 18.42 |
| tel:+447451261333 | /contact/ | 103.66 × 15 |

Also under 24px (not named in the ticket, same cause): footer email 152.77 × 18; homepage `.arrow-link` CTAs 171.84 × 21.11 and 147.27 × 21.11. Footer nav column links were already 350 × 25.81. Hamburger 44 × 44.

### q-auto-4f45 — tagline aria-label on `<p>` — CONFIRMED

Homepage `.hero__typed` is a `<p aria-label="It's not magic. It's very good engineering.">`. axe-core 4.12.1 reports this as **incomplete** (not `violations[]`), rule `aria-prohibited-attr`, impact serious, 1 node. Homepage counts: 0 violations / 2 incomplete / 41 passes / 48 inapplicable. Contact was not axe'd in the live pass (navigated to terms in the same turn). Terms live: 0 violations / 1 incomplete (color-contrast only) / 39 passes / 50 inapplicable.

The prohibited attribute is present. Not already fixed. Violation count is already 0; the defect is the prohibited attr sitting in incomplete.

### q-auto-acf7 — terms date — CONFIRMED

https://automancer.uk/terms/ loaded (`title` Website Terms \| Automancer, h1 Website Terms). Main text has no "Last updated", no `time[datetime]`, no August/July 2026. Premise holds.

### q-auto-e7d0 — footer logo eager — CONFIRMED

`footer img.footer__mark` `loading` attribute is `null` (eager default) on home, contact, and terms. `src=/assets/images/logo.svg`.

## Date derivation (q-auto-acf7)

Command: `git log -1 --format=%cs -- src/pages/terms.astro` → **2026-08-21**.

That commit is `d412e77` ("fix: correct accessibility and SEO defects…") and the terms.astro diff is **one line**: the meta description lengthened. The terms body (`You may browse and use this website…`) was introduced in `1f74245` on **2026-07-03** and has not changed since.

History is therefore ambiguous between file-mtime (21 August 2026) and substance (3 July 2026). Matching `/privacy/`'s rule that lastUpdated records substance, not meta-description tweaks, the published date is **3 July 2026**, with `<time datetime="2026-07-03">`.

## Fixes applied (source)

1. `.footer__legal a`, `.footer__addr a`, `.arrow-link`, `.form__micro a[href^="tel:"]` get `min-height/min-width: 24px` and `inline-flex` alignment. No footer redesign: same type, colour, gap, copy.
2. Homepage tagline: removed `aria-label` from the `<p>`. Accessible text is a `.sr-only` span (real text content). Visual typed line + caret stay `aria-hidden`.
3. Terms: `lastUpdated: '3 July 2026'` in `src/data/site-content.ts`; page renders `Last updated: <time datetime="2026-07-03">3 July 2026</time>` in the same `sys sys--spectral` treatment as /privacy/. Terms body copy untouched.
4. Footer mark: `loading="lazy"`. Nav mark left eager (above the fold).

## After measurements (local preview, visitor browser)

Preview: `http://127.0.0.1:42933` (`astro preview --host 127.0.0.1 --port 42933`). Same session `site-a11y-20260830`, viewport 390×844. Page-load controls: titles/h1s as below, `readyState === complete`.

### Tap targets (q-auto-8cd1)

| target | before (live) | after (preview) |
| --- | --- | --- |
| Privacy | 48.38 × 18.42 | 48.38 × **24** |
| Terms | 34.56 × 18.42 | 34.56 × **24** |
| llms.txt | 55.28 × 18.42 | 55.28 × **24** |
| contact tel | 103.66 × 15 | 103.66 × **24** |

Homepage and contact: zero remaining `a, button` with both dimensions > 0 and either < 24.

Positive control (same `getBoundingClientRect` loop): planted `#tap-positive-control` at 10×10; the loop returned **1 row** (`id=tap-positive-control`, 10×10). Plant removed. Empty after-list is not a failed-to-measure.

### axe (q-auto-4f45) — axe-core **4.12.1** via `agent-browser a11y --json`

Pages run: `/` (live + preview), `/contact` (preview), `/terms/` (live + preview).

| page | when | violations | incomplete | passes | inapplicable |
| --- | --- | ---: | ---: | ---: | ---: |
| `/` live | before | 0 | 2 (aria-prohibited-attr + color-contrast) | 41 | 48 |
| `/` preview | after | 0 | 1 (color-contrast only; gradients / pseudo) | 41 | 48 |
| `/contact` preview | after | 0 | 1 (color-contrast) | 43 | 46 |
| `/terms/` live | before | 0 | 1 (color-contrast) | 39 | 50 |
| `/terms/` preview | after | 0 | 1 (color-contrast) | 39 | 50 |

`aria-prohibited-attr` is gone from homepage incomplete. Remaining incomplete is color-contrast axe cannot compute over the spectrum/hatch (not a rule we loosened).

Positive control for a zero: prepended `<p id="axe-positive-control" aria-label="planted prohibited label">planted</p>` on the fixed homepage and re-ran the **identical** `agent-browser a11y --json`. Result: `aria-prohibited-attr` incomplete **1 node** (`#axe-positive-control`) plus a `region` violation for the planted node. Plant removed. A clean run is therefore not “page failed to load”.

Tagline after: `<p class="hero__typed">` has `aria-label: null`; `.sr-only` text is `It's not magic. It's very good engineering.`; `[data-typed]` is `aria-hidden="true"`.

### Terms date (q-auto-acf7)

Rendered on preview `/terms/`: parent text `Last updated: 3 July 2026`, `<time datetime="2026-07-03">`. Also in `dist/llms-full.txt` (`Last updated: 3 July 2026`) and `dist/api/pages.json` (`lastModified: "3 July 2026"`). Terms body copy unchanged.

### Footer logo (q-auto-e7d0)

Preview home/contact/terms: `footer img.footer__mark` `loading="lazy"`. Live before: `loading: null`.

## Gates (one `campaign heavy --label site-a11y-20260830`)

Waited 621s for a slot, then ran check + test + both verify-production paths inside that one invocation. Wrapper exit 0.

### `pnpm run verify` — VERIFY_EXIT=0

- `astro check`: **81 files, 0 errors / 0 warnings / 0 hints**. Read from the `Result (81 files):` block.
- `vitest run` v4.1.11: **Test Files 13 passed / 0 failed / 0 skipped**; **Tests 133 passed / 0 failed / 0 skipped**. Read from the vitest summary (`Test Files  13 passed (13)` / `Tests  133 passed (133)`). Includes page-weight budgets (no budget loosened).

### `ops/verify-production.sh --self-test` — SELFTEST_EXIT=0

- Printed: `SELF-TEST RESULT: all 22 locally isolatable assertions across 9 checks can fail AND can pass.`
- Counted `SELFTEST PASS` vs `SELFTEST FAIL` in that output: **44 passed / 0 failed / 0 skipped** (22 fail-direction + 22 pass-direction). Exit 0.

### `ops/verify-production.sh https://automancer.uk` — PRODVERIFY_EXIT=0

- Counted lines matching `^PASS  ` vs `^FAIL  `: **16 passed / 0 failed / 0 skipped**. Printed `RESULT: all checks passed against https://automancer.uk`. This is the live site as it stands today (our branch is not deployed).

## Cleanup

- `agent-browser close --session site-a11y-20260830`. `agent-browser session list` afterwards: `iqm-enquiry-screen`, `bpg-finish-short-phone` only (other agents). `session info --json --session site-a11y-20260830` → `active: false`, `pid: null`. Never `close --all`.
- `astro preview stop` reaped pid 3845820; `/proc/3845820` gone; `astro preview status` → no preview server.

---

# Report

## 1. One row per item

| id | premise vs live site | change |
| --- | --- | --- |
| q-auto-8cd1 | **Confirmed.** Privacy 48.38×18.42, Terms 34.56×18.42, llms.txt 55.28×18.42, contact tel 103.66×15 at 390×844. | `min-height/min-width: 24px` + `inline-flex` on footer legal links, footer address links, `.arrow-link`, contact `tel:` links. Footer type, colour, gap, copy unchanged. |
| q-auto-4f45 | **Confirmed.** `.hero__typed` is a `<p>` with `aria-label`. axe 4.12.1 flags `aria-prohibited-attr` as incomplete/serious (not in `violations[]`). | Removed `aria-label`. Accessible text is a `.sr-only` span (real text). Visual typed line `aria-hidden`. |
| q-auto-acf7 | **Confirmed.** `/terms/` has no last-changed date. | Published `Last updated: 3 July 2026` with `<time datetime="2026-07-03">`. Body text not edited. See date derivation above. |
| q-auto-e7d0 | **Confirmed.** Footer mark `loading` is null (eager). | `loading="lazy"` on `.footer__mark`. Nav mark left eager. |

None of the four were already fixed.

## 2. Before and after measurements

Tap targets, axe counts, terms date, logo `loading`: tables in “Live-site premises” and “After measurements” above. Tool versions: agent-browser 0.34.0, axe-core 4.12.1.

## 3. Positive controls for every zero

- **No remaining small standalone targets:** identical measurement against a planted 10×10 `#tap-positive-control` returned 1 row; after removal, 0 rows.
- **0 axe violations / no aria-prohibited-attr:** identical `agent-browser a11y --json` against a planted `<p aria-label>` returned `aria-prohibited-attr` (1 node, incomplete) plus a `region` violation. Pages actually loaded: titles, h1s, `Company No. 17060907` / “Website Terms”, `readyState=complete`, matching URLs.

## 4. Every gate, with counts

How counts were read: vitest summary lines; `astro check` Result block; `SELFTEST PASS`/`FAIL` line counts; `^PASS  `/`^FAIL  ` line counts on production verify.

| gate | passed | failed | skipped | exit |
| --- | ---: | ---: | ---: | ---: |
| astro check (81 files) | 81 (0 errors, 0 warnings, 0 hints) | 0 | 0 | 0 |
| vitest (files) | 13 | 0 | 0 | 0 |
| vitest (tests) | 133 | 0 | 0 | 0 |
| verify-production.sh --self-test | 44 (22×fail-dir + 22×pass-dir) | 0 | 0 | 0 |
| verify-production.sh https://automancer.uk | 16 | 0 | 0 | 0 |

`campaign heavy` wrapper exit 0. Page-weight budgets still the measured+headroom values in `tests/performance.test.ts`; none raised.

## 5. What I did NOT do

- Did not deploy.
- Did not redesign, rebrand, or move logo/palette/type.
- Did not invent a terms date; did not edit terms body.
- Did not loosen a page-weight budget or an axe rule. Color-contrast incomplete checks remain (axe cannot compute over spectrum/hatch); not treated as a defect to restyle.
- Did not touch `/opt/automancer/ops`.
- Did not skip any of the four: all four were still present on the live site.
- Did not `close --all` the shared browser daemon.
- Did not commit `.impeccable/` (session hook ignore for the pre-existing `.rung::before` side accent, which this lane is forbidden to restyle).


